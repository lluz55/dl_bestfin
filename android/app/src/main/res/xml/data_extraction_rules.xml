<?xml version="1.0" encoding="utf-8"?>
<!--
  Regras de extração para Android 12+ (android:dataExtractionRules).
  Bloqueia tanto o backup em nuvem quanto a transferência device-to-device
  dos dados financeiros locais (SQLite), arquivos e preferências. Os dados
  do usuário só saem do dispositivo pelo fluxo de backup explícito do app.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="database" path="." />
        <exclude domain="file" path="." />
        <exclude domain="sharedpref" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="database" path="." />
        <exclude domain="file" path="." />
        <exclude domain="sharedpref" path="." />
    </device-transfer>
</data-extraction-rules>
